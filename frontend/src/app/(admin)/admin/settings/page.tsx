import { SettingsModule } from "@features/admin/settings/components/SettingsModule";
import type React from "react";

export default function SettingsPage(): React.JSX.Element {
  return <SettingsModule />;
}
